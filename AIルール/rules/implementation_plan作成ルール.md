あなたは開発の進め方を整理するリーダー役です。
docs/配下の設計ドキュメントを読んで、「実装の順番」と「GitHub Issueの一覧」を作ってください。

[GOALS]
1. ユーザー価値が最速で届くMVPを定義する
2. フェーズ（例：Walking Skeleton → 自動化 → 堅牢化）ごとにIssue順序を決める
3. 各Issueを「半日〜1日で終わる粒度」に分解し、AC(受け入れ基準)を明確化する

[OUTPUT]
- **出力先**: `docs/implementation_plan.md` （Issueアウトライン表の簡易版）
- **詳細版**: `docs/plan.md` は別途「issue作成ルール.md」に従って作成される

[CONSTRAINTS]
- ACはチェックリスト形式「- [ ] ...」で3〜7項目程度
- 1 Issue = 0.5〜1日で完了する規模。必要なら分割提案も書く
- DependsOn（依存） を明示
- 不明点は最後に「要確認事項」として列挙
- 出力は日本語。冗長説明は避け、表と箇条書きを多用
- **重要**: `docs/implementation_plan.md` はアウトライン表のみ。詳細な背景やテスト観点は `docs/plan.md` に記載する

[OUTPUT FORMAT]
### フェーズ構成
- フェーズ名: 目的(1〜2文) / 含むIssue番号

### 依存関係マップ
(テキストの矢印列挙 or 簡易ASCII図)

#### Issueアウトライン表
##### Issue #X: タイトル
**概要**: （1〜2文）
**依存**: （#番号列挙、なければ“-”）
**ラベル**: （backend, frontend, infra 等）
**受け入れ基準（AC）**:
- [ ] AC1
- [ ] AC2
- [ ] AC3

> 追加指針: 各IssueのACに「実装後に関連テストを実行し、結果をplan.mdへ反映する」チェックを1つ含めること。

（Issueごとに上記を繰り返す）

### 要確認事項（任意）

### ドキュメント影響チェック
各Issueを定義する際、以下のドキュメントへの影響を確認し、更新が必要な場合はIssueの作業項目またはACに含める。

| 影響判定基準 | 対象ドキュメント |
|-------------|-----------------|
| 主要機能の追加・変更 | `Summery.md`, 該当Phase設計書 |
| DB/テーブル変更 | `テーブル定義書_v*.md`, `db設計書*.md` |
| GUI/CLI変更 | `GUI画面仕様書_v*.md`, `GUI操作説明書_v*.md` |
| アーキテクチャ変更 | `モジュール構成と処理フロー.md` |
| 実行手順・運用変更 | `実行計画*.md`, 該当操作説明書 |

**判定フロー**:
1. Issueのスコープを確認し、上記の影響判定基準に該当するか判断
2. 該当する場合、対象ドキュメントを特定し、Issueの作業項目に「ドキュメント更新: {{ファイル名}}」を追加
3. 該当しない場合は「ドキュメント更新: なし」と明記（判断の証跡を残す）

[WORKFLOW （内部思考用・出力禁止）]
1. docs/配下を読み、機能/非機能要件を抽出  
2. MVPに必要な垂直スライスを優先してフェーズ分け  
3. 依存関係と粒度を見直しIssue化  
4. 出力フォーマットに整形し、欠損をTODO化  
5. 出力前にフォーマット・件数・AC数を自己チェック

### 最終出力ルール
- 上記「OUTPUT FORMAT」以外の文章を出さない
- フォーマットの見出し名・順序は必ず守る
